[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: Fast isolated tests for services and helpers.
    integration: API integration tests for FastAPI endpoints.
    e2e: End-to-end business flows across multiple modules.
addopts = -ra --strict-markers
